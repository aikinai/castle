Host *
    ControlMaster auto
    ControlPath ~/.ssh/%r@%h:%p
    ControlPersist yes
    IdentityFile ~/.ssh/id_ed25519

# Per-machine hosts (not in git). Create with:
#   touch ~/.ssh/config.local && chmod 600 ~/.ssh/config.local
Include config.local
