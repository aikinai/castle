" Enable syntax highlighting.
syntax on

" Always show status line even with one window
set laststatus=2

" Allowed unsaved buffers to go into background
set hidden

" Keep the cursor 10 lines from the top and 10 lines from the bottom
set scrolloff=10

" Allow the cursor to navigate empty space in visual block mode
set virtualedit=block
