" Enable search highlighting
set hlsearch

" Map ;n to disable search highlight
nmap <silent> <leader>n :nohls<CR>

" Incrementally match searches
set incsearch

" Set the search scan to wrap lines
set wrapscan

" Search case insensitive, but sensitive if an uppercase character is used
set ignorecase
set smartcase
